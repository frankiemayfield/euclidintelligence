import { SubLayout } from "@/components/sub/SubLayout";
import { AlertTriangle, CheckCircle2 } from "lucide-react";
import { useDemoProject } from "@/hooks/use-demo-project";
import { money } from "@/data/demoUniverse";
export default function SubEstVsActualPage(){const {project,quote}=useDemoProject();const active=Boolean(project.actuals);const invoiced=project.actuals?.transactions.filter(transaction=>transaction.description.toLowerCase().includes("trueframe")).reduce((sum,transaction)=>sum+transaction.amount,0)??0;return <SubLayout><div className="max-w-5xl p-6 lg:p-8"><div className="mb-8"><h1 className="font-display text-2xl font-bold">Estimate vs Actual</h1><p className="mt-1 text-sm text-muted-foreground">{project.name} — TrueFrame framing performance</p></div>{!active?<div className="odyssey-surface rounded-2xl p-10 text-center"><AlertTriangle className="mx-auto mb-3 text-warning"/><h2 className="font-display text-lg font-semibold">Job costs are not available yet</h2><p className="mx-auto mt-2 max-w-lg text-sm text-muted-foreground">This quote is currently {project.subStatus}. Cost performance begins after TrueFrame is awarded and invoices are recorded.</p></div>:<><div className="mb-6 grid grid-cols-3 gap-4"><div className="odyssey-surface rounded-xl p-5 text-center"><p className="text-xs text-muted-foreground">Awarded Quote</p><p className="font-display text-2xl font-bold">{money(quote.currentAmount)}</p></div><div className="odyssey-surface rounded-xl p-5 text-center"><p className="text-xs text-muted-foreground">Invoiced to Date</p><p className="font-display text-2xl font-bold">{money(invoiced)}</p></div><div className="odyssey-surface rounded-xl p-5 text-center"><p className="text-xs text-muted-foreground">Remaining</p><p className="font-display text-2xl font-bold text-primary">{money((quote.currentAmount??0)-invoiced)}</p></div></div><div className="odyssey-surface rounded-xl p-5"><h2 className="mb-3 font-display font-semibold">Cost Status</h2><p className="flex items-center gap-2 text-sm"><CheckCircle2 size={14} className="text-primary"/>Current progress invoices are mapped to the awarded {project.name} carpentry package.</p></div></>}</div></SubLayout>}
